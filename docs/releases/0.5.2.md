# 0.5.2 — runtime hotkeys, SCMI CPUFreq and IR Torch integration

- Keep the user-facing profile order `Whisper < Quiet < Normal < Turbo < Full Speed`.
- Keep Quiet / Normal / Turbo / Full Speed as native ASUS firmware modes; Whisper remains the only synthetic acoustic-first policy.
- Fix Fn+F deployment by loading the EC module before the HID module that consumes its exported profile-cycle symbol, and unloading HID before EC. Unload failures are no longer silently ignored during upgrades.
- Keep the X1E SCMI CPUFreq clients loadable on the current 7.1.5 Ubuntu kernel; verified hardware runtime exposes three SCMI policies for CPUs 0-3, 4-7 and 8-11 with a 3.4176 GHz hardware maximum.
- Add the Aegis Hello 0.8.3 V4L2 Torch patch for continuous IR. Flash/strobe stays unchanged.
- Archive the exact Resources A14/MSM/Qualcomm patch artifacts with SHA-256 checksums while retaining the newer maintained repair scripts.
